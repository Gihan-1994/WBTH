// Export commonly used Prisma types for reuse across the monorepo
export type { User, Tourist, Guide, Booking, Accommodation, Event, Payment, Notification } from '@prisma/client';
export { UserRole, BookingType, BookingStatus } from '@prisma/client';
